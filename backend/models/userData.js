class UserData{
    constructor(userId, username, email, isAdmin)
    {
        this.userId = userId;
        this.username = username;
        this.email = email;
        this.isAdmin = isAdmin;
    }
}

module.exports = UserData
